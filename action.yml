name: 'Add to Project Board'
description: 'Adds new Issues and Pull Requests to a Project Board'
inputs:
  column_id:
    description: 'Column ID of Project Board'
    required: true
runs:
  using: 'docker'
  image: 'docker.pkg.github.com/bensheldon/action-add-to-project/action-add-to-project:latest'
